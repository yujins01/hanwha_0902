# FastAPI

---

💡**FastAPI란?**

> python으로 API(서버)를 만들 수 있게 해주는 웹 프레임워크

- API: 데이터를 주고 받는 약속/통신 창구

#### 설치 및 실행 방법

- 패키지 설치

```jsx
pip install "fastapi[standard]"
```

- 실행 명령어 ([파일명.py](http://xn--v42b30vy3i.py/))

```jsx
fastapi dev [main.py](<http://main.py/>)
```

- 실행 명령어 (파일명\:app)
- 코드를 저장하면서 변경사항을 감지해서 서버를 자동으로 재실행
- pip install uvicorn 설치하여 unicorn 명령어 사용

```jsx
uvicorn main:app --reload
```

#### 주요 옵션 설명

- main : 파일명
- app: 코드 내부의 FastAPI 인스턴스 변수 (app = FastAPI())
- **-reload**: 코드가 수정될 때마다 서버를 자동으로 재시작 (개발용)
- **-host 0.0.0.0**: 외부 접속 허용
- **-port 8000**: 포트 번호 변경 (기본값: 8000)
  - 포트를 변경하고 싶은 경우
  - uvicorn main\:app --port 8001 --reload

#### FastAPI API 문서

1. 일반 API
   - `http://localhost:8000/`
   - **서버에 정의된 API 주소로 요청을 보내 데이터를 주고받을 수 있음**
2. 대화형 API 문서
   - `http://localhost:8000/docs`
   - **API를 확인하고 직접 테스트하는 문서 화면**
   - **Try it out → Execute**를 눌러 실제 요청을 보내볼 수 있음

#### 메서드 정리

| 메서드 | 의미 | 용도 |
| ---------- | ----- | ----------------------- |
| **GET**    | 가져오기  | 서버의 데이터 조회 |
| **POST**   | 보내기   | 서버에 데이터 전달 / 새로운 데이터 생성 |
| **PUT**    | 전체 수정 | 기존 데이터 전체 수정 |
| **PATCH**  | 부분 수정 | 기존 데이터 일부 수정 |
| **DELETE** | 삭제    | 기존 데이터 삭제 |

#### 요청과 응답

- **요청(Request)** : 클라이언트가 서버에 데이터를 요청하거나 전달하는 것 get
- **응답(Response)** : 서버가 요청에 대한 결과를 돌려주는 것 post

#### Get 요청

- Get: 서버에서 데이터를 가져오는 요청 (가져오는 것)

예를 들어:

```
@app.get("/users")
def get_users():
	return ["철수","영희"]
```

브라우저에서 로 요청하면,

```
<http://127.0.0.1:8000/users>
```

응답 받음

```
["철수","영희"]
```

#### Post 요청

- POST: 서버에 데이터를 전달하는 요청 (보내는 것)
- Body(본문)에 데이터를 담아 서버로 보냄

```python
@app.post("/items/")
async def create_item(item: Item):
    return item
```

#### 경로 Path

- FastAPI에서 URL의 경로에 따라 실행할 함수
- 같은 경로를 중복해서 만들면 먼저 선언된 경로가 우선적으로 매칭
- 같은 경로를 중복해서 만들지 않는게 좋음

#### 쿼리 매개변수

- URL에서 `?` 뒤에 붙는 값
- `&`로 여러 개를 구분

```
<http://127.0.0.1:8000/items/**?skip=0&limit=10**>
```

- `skip`: 값 `0`을 가집니다.
- `limit`: 값 `10`을 가집니다.
- 값을 입력하지 않으면 코드에서 설정한 기본값이 사용
- 사용자가 URL 쿼리 매개변수로 원하는 값을 직접 지정 가능

#### 동기 비동기

- Sync (동기) : 작업이 끝날 때까지 기다림
- Async(비동기): 기다리는 동안 다른 작업을 처리할 수 있음

#### HTTP 상태 코드

| 코드 | 의미 | 쉽게 |
|---|---|---|
| 2xx | 성공 | ✅ 요청 성공 |
| 3xx | 리다이렉션 | 🔄 다른 곳으로 이동 |
| 4xx | 클라이언트 요청 오류 | ❌ 내가 요청을 잘못함 |
| 5xx | 서버 오류 | 💥 서버에서 문제가 발생 |
